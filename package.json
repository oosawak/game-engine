{
  "name": "game-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "setup:ardy-engine": "node scripts/bootstrap-ardy-engine.mjs",
    "serve:ardy-engine": "node scripts/launch-ardy-engine.mjs",
    "export:web": "node scripts/export-web.mjs",
    "serve:text-to-vrma": "node scripts/launch-text-to-vrma.mjs",
    "serve:text-to-vrma:static": "node scripts/serve-text-to-vrma.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
