# Standard VRM Asset

このディレクトリは、VRM Editor と実行時確認で使う標準アバターの公開配置先です。

推奨ファイル名:

- `standard.vrm`
- `standard.glb`

運用ルール:

- この場所には、公開してよい確認用アセットだけを置く
- 編集元の原本を分けたい場合は、別途 `assets/vrm/standard/` を使う
- VRM Editor はまずこの標準アセットを読み込み対象にする
- 標準アセットが無い場合のみ、一時的なフォールバック表示を使う

